import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles(theme => ({}));

function Feets() {
  return (
    <div className='App'>
      <h1>hello world</h1>
    </div>
  );
}

export default Feets;
